"use client"

import React from 'react';
import AppDashboard from './components/dashboard/AppDashboard';

export default function Home() {
  // Directly render the main application dashboard for the demo
  return <AppDashboard />;
}